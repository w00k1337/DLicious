/**
 * Job Implementations
 *
 * This module exports all job implementations and handles job registration
 */

// TODO: Import and export job implementations in T5 tasks
// export * from './stash-performer-sync'
// export * from './stash-scene-sync'
// export * from './metadata-fetch'
// export * from './download-status-check'

/**
 * Register all jobs with the queue system
 * This function will be implemented when job handlers are created
 */
export const registerAllJobs = (): void => {
  // TODO: Implement job registration in T5 tasks
  // jobRegistry.register(stashPerformerSyncJob)
  // jobRegistry.register(stashSceneSyncJob)
  // jobRegistry.register(metadataFetchJob)
  // jobRegistry.register(downloadStatusCheckJob)
}
